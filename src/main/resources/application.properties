spring.application.name=hospital-backend

# Database configuration (using environment variables with local fallbacks)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-bitter-glitter-amax9jk1-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_Gxf56oaNLjtU}

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security / CORS
cors.allowed.origins=${ALLOWED_ORIGINS:http://localhost:5173}

# Server configuration
server.port=${PORT:8080}
